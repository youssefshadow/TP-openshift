name: OpenShift Deploy

on:
  push:
    # On met les deux pour être sûr de couvrir ta branche
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to OpenShift
        uses: redhat-actions/oc-login@v1
        with:
          # Noms de paramètres exacts pour cette version de l'action
          openshift_server_url: ${{ secrets.OPENSHIFT_SERVER }}
          openshift_token: ${{ secrets.OPENSHIFT_TOKEN }}
          namespace: ${{ secrets.OPENSHIFT_NAMESPACE }}
          # Important : ignore l'erreur de certificat du cluster de TP
          insecure_skip_tls_verify: true

      - name: Start backend build (json-server)
        run: |
          oc start-build json-server --from-dir=docker/backend --follow

      - name: Start frontend build (Angular)
        run: |
          oc start-build frontend --from-dir=. --follow
